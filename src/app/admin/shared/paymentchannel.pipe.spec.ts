import { PaymentchannelPipe } from './paymentchannel.pipe';

describe('PaymentchannelPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentchannelPipe();
    expect(pipe).toBeTruthy();
  });
});
